<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>TripMakers Ticket #{{ billet.numero }}</title>
    <style>
        @page {
            margin: 0;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
            color: #333;
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }
        .ticket-container {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
            background: white;
        }
        .ticket {
            position: relative;
            border: 2px solid #3554D1;
            border-radius: 16px;
            overflow: hidden;
            margin: 20px;
            background: white;
            box-shadow: 0 10px 30px rgba(53, 84, 209, 0.1);
        }
        .ticket-header {
            background: linear-gradient(135deg, #00355F 0%, #005AA9 100%);
            background: linear-gradient(135deg, #3554D1 0%, #0b2786 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        .ticket-header::before, .ticket-header::after {
            content: '';
            position: absolute;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            z-index: 0;
        }
        .ticket-header::before {
            top: -100px;
            left: -100px;
        }
        .ticket-header::after {
            bottom: -100px;
            right: -100px;
        }
        .ticket-header h1, .ticket-header h2 {
            position: relative;
            z-index: 1;
            margin: 0;
            font-weight: 700;
        }
        .ticket-header h1 {
            font-size: 32px;
            margin-bottom: 5px;
        }
        .ticket-header h2 {
            font-size: 18px;
            opacity: 0.9;
        }
        .logo-container {
            background-color: white;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin: 0 auto 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            position: relative;
            z-index: 1;
        }
        .logo {
            padding: 15px;
            text-align: center;
        }
        .logo h2 {
            color: #3554D1;
            margin: 0;
            font-size: 24px;
            font-weight: 800;
        }
        .logo p {
            color: #697488;
            margin: 5px 0 0;
            font-size: 14px;
        }
        .ticket-body {
            padding: 30px;
            position: relative;
        }
        .activity-image {
            width: 100%;
            height: 200px;
            border-radius: 12px;
            object-fit: cover;
            margin-bottom: 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }
        .activity-badge {
            display: inline-block;
            background: linear-gradient(to right, #F5A623, #F27121);
            color: white;
            padding: 6px 14px;
            border-radius: 30px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 16px;
            box-shadow: 0 2px 4px rgba(242, 113, 33, 0.3);
        }
        .activity-details {
            margin-bottom: 30px;
        }
        .heading {
            color: #00355F;
            font-size: 26px;
            margin: 0 0 8px 0;
            font-weight: 700;
            letter-spacing: -0.5px;
            font-family: 'Montserrat', 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }
        .subheading {
            font-size: 16px;
            color: #555;
            display: flex;
            align-items: center;
            gap: 8px;
            font-weight: 500;
            letter-spacing: 0.2px;
        }
        .subheading i {
            margin-right: 5px;
            color: #3554D1;
        }
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 30px;
        }
        .info-item {
            padding: 15px;
            background-color: #f5f8fe;
            border-radius: 12px;
        }
        .info-item .label {
            font-size: 12px;
            color: #777;
            margin-bottom: 4px;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 600;
        }
        .info-item .value {
            font-size: 16px;
            color: #333;
            font-weight: 600;
            letter-spacing: -0.2px;
        }
        .divider {
            border: none;
            height: 2px;
            background: repeating-linear-gradient(to right, #3554D1 0, #3554D1 5px, transparent 5px, transparent 12px);
            margin: 30px 0;
        }
        .qr-section {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-top: 30px;
        }
        .qr-container {
            flex: 0 0 160px;
            text-align: center;
        }
        .qr-image {
            max-width: 160px;
            height: auto;
            margin-bottom: 10px;
            border-radius: 8px;
        }
        .qr-instruction {
            font-size: 13px;
            color: #697488;
            margin-top: 5px;
            max-width: 160px;
        }
        .ticket-info {
            flex: 1;
            padding-left: 30px;
        }
        .info-row {
            margin-bottom: 15px;
            display: flex;
            align-items: flex-start;
        }
        .info-row .label {
            width: 45%;
            color: #697488;
            font-size: 14px;
        }
        .info-row .value {
            width: 55%;
            color: #051036;
            font-size: 15px;
            font-weight: 600;
            text-align: right;
        }
        .status {
            display: inline-block;
            padding: 5px 15px;
            border-radius: 30px;
            font-size: 14px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .status-confirmed {
            background-color: rgba(40, 167, 69, 0.15);
            color: #28a745;
        }
        .status-pending {
            background-color: rgba(255, 193, 7, 0.15);
            color: #ffc107;
        }
        .status-cancelled {
            background-color: rgba(220, 53, 69, 0.15);
            color: #dc3545;
        }
        .ticket-footer {
            background-color: #F5F8FE;
            padding: 20px;
            text-align: center;
            font-size: 14px;
            color: #697488;
            border-top: 1px dashed #e0e4f6;
        }
        .ticket-number {
            font-family: 'Courier New', monospace;
            font-size: 16px;
            letter-spacing: 1px;
            color: #3554D1;
            font-weight: 700;
            margin-top: 5px;
        }
        .tear-line {
            position: relative;
            height: 20px;
            margin: 0;
            overflow: hidden;
        }
        .tear-line::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            border-top: 2px dashed #e0e4f6;
        }
        .tear-line::after {
            content: '✂';
            position: absolute;
            top: -7px;
            right: 20px;
            color: #697488;
            font-size: 16px;
            transform: rotate(90deg);
        }
    </style>
</head>
<body>
    <div class="ticket-container">
        <div class="ticket">
            <div class="ticket-header">
                <div class="logo-container">
                    <div class="logo">
                        <h2>TM</h2>
                    </div>
                </div>
                <h1>Travel E-Ticket</h1>
                <h2>#{{ billet.numero }}</h2>
            </div>
            
            <div class="ticket-body">
                {% if activityImage %}
                <img src="{{ asset('uploads/activities/' ~ activityImage) }}" alt="{{ activity.activityName }}" class="activity-image">
                {% endif %}
                
                <div class="activity-badge">{{ activity.activityGenre }}</div>
                
                <div class="activity-details">
                    <h3 class="heading">{{ activity.activityName }}</h3>
                    <div class="subheading">
                        <i>📍</i> {{ activity.activityDestination }}
                    </div>
                </div>
                
                <div class="info-grid">
                    <div class="info-item">
                        <div class="label">Booking Date</div>
                        <div class="value">{{ reservation.dateAchat|date('d M Y') }}</div>
                    </div>
                    <div class="info-item">
                        <div class="label">Participants</div>
                        <div class="value">{{ reservation.nombre }} person(s)</div>
                    </div>
                    <div class="info-item">
                        <div class="label">Price per Person</div>
                        <div class="value">{{ reservation.prixUnite }} TND</div>
                    </div>
                    <div class="info-item">
                        <div class="label">Total Amount</div>
                        <div class="value">{{ reservation.prixTotal }} TND</div>
                    </div>
                </div>
                
                <div class="tear-line"></div>
                
                <div class="qr-section">
                    <div class="qr-container">
                        <img src="{{ qrCode }}" alt="QR Code" width="150" height="150" style="width:150px; height:150px; border:1px solid #ddd;">
                        <div class="qr-instruction">Scan to view activity details</div>
                    </div>
                    
                    <div class="ticket-info">
                        <div class="info-row">
                            <div class="label">Passenger:</div>
                            <div class="value">{{ user.name }} {{ user.lastname }}</div>
                        </div>
                        <div class="info-row">
                            <div class="label">Email:</div>
                            <div class="value">{{ user.email }}</div>
                        </div>
                        <div class="info-row">
                            <div class="label">Issue Date:</div>
                            <div class="value">{{ issueDate|date('d M Y, H:i') }}</div>
                        </div>
                        <div class="info-row">
                            <div class="label">Status:</div>
                            <div class="value">
                                <span class="status status-{% if reservation.statuts == 'confirmed' %}confirmed{% elseif reservation.statuts == 'pending' %}pending{% else %}cancelled{% endif %}">
                                    {{ reservation.statuts|capitalize }}
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="ticket-footer">
                <div>TripMakers - Your Adventure Awaits</div>
                <div class="ticket-number">REF: {{ billet.numero }}</div>
            </div>
        </div>
    </div>
</body>
</html>
